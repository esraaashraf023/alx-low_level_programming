#include "main.h"
/**
 *_abs - computes the absolute value of the integer argument
 *
 *@n: checks the number
 *
 * Return: 0
 */

int _abs(int n)
{

	if (n < 0)
	{
		return (-n);
	}
	else
		return (n);
}
